import { Env } from "../../../types";

export default async function AddFavorite(req : Request, env : Env) {
    // todo: add the favorite to the users firebase
    // todo: add the favorite to the business's favorites num

    try {

    } catch(err) {

    }
    
}