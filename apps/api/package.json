{
    "name": "@locora/api",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "npm run build && wrangler deploy",
        "deploy": "wrangler deploy",
        "build": "tsc"
    },
    "devDependencies": {
        "wrangler": "^3.50.0",
        "typescript": "^5.5.0",
        "@cloudflare/workers-types": "^4.2025.0"
    },
    "dependencies": {
        "itty-router": "^4.0.21"
    }
}
