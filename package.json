{
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix apps/site/locora\" \"npm run dev --prefix apps/api\" \"npm run dev --prefix apps/app/locora\""
    },
    "devDependencies": {
        "concurrently": "^9.2.1",
        "wrangler": "^4.50.0"
    },
    "dependencies": {
        "react": "^18.3.1",
        "react-dom": "^18.3.1"
    }
}
